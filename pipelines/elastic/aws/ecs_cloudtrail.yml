priority: 30
transformations:
  - id: field_mapping
    type: field_name_mapping
    mapping:
      errorMessage:
        - error.message
      userName:
        - user.name
      eventSource:
        - aws.cloudtrail.event_source
      eventName:
        - event.action
      eventTime:
        - '@timestamp'